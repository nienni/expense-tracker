<h1>＊必填欄位 請輸入你的支出</h1>
<div class="container">
  <form action="/records/{{{record._id}}}/edit" method="POST">
    <div>
      <p>名稱:</p>
      <input type="text" name="name" value="{{{record.name}}}" placeholder="{{{record.name}}}">
    </div>

    <div>
      <p>日期:</p>
      <input type="text" name="date" id="date" placeholder="{{{record.date}}}" class="textbox-n"
        onfocus="(this.type='date')">
    </div>

    <div>
      <p>類別:</p>
      <select class="custom-select" name="category" aria-placeholder="{{{record.category}}}">
        <option selected>{{{record.category}}}</option>
        <option value="家居物業">家居物業</option>
        <option value="交通出行">交通出行</option>
        <option value="休閒娛樂">休閒娛樂</option>
        <option value="餐飲食品">餐飲食品</option>
        <option value="其他">其他</option>
      </select>
    </div>

    <div>
      <p>金額:</p>
      <input type="number" name="amount" value="{{{record.amount}}}" placeholder="{{{record.amount}}}">
    </div>


    <button type="submit">edit</button>
    <a href="/">back</a>
    <form action="/records/{{{record._id}}}/delete" method="POST" style="display: inline;">
      <button type="submit">delete</button>
    </form>
  </form>
</div>